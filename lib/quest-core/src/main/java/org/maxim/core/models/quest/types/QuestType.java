package org.maxim.core.models.quest.types;

public enum QuestType {
    // In order of most easy to complete (Ascending)
    Unknown,
    Achievement,
    Stream,
    Activity,
    Play,
    Watch,
}
